
@{
    ViewBag.Title = "_NavbarSis";
}
<!-- Navbar -->


@using Microsoft.AspNet.Identity

@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>

    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#">
                <i class="far fa-user"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                <div class="dropdown-divider"></div>
                <a href="@Url.Action("Index","Manage")" class="dropdown-item" style="margin-left:13px">
                    <i class="fa fa-user-doctor"></i>
                    Administrar Cuenta

                    @*@Html.ActionLink("Administrar Cuenta", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })*@
                </a>
                <div class="dropdown-divider"></div>
                <a href="javascript:document.getElementById('logoutForm').submit()" class="dropdown-item" style="margin-left:13px"> <i class="fa fa-lock"></i>   Cerrar sesión</a>
            </div>
        </li>
    </ul>

</nav>

            }
            }
            else
            {

            <li class="nav-item d-none d-sm-inline-block" style="margin:6px">@Html.ActionLink("Registrarse", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <br>
            <li class="nav-item d-none d-sm-inline-block">@Html.ActionLink("Iniciar sesión", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
            }
        <!-- /.navbar -->

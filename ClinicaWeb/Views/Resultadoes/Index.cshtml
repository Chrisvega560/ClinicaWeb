@model IEnumerable<ClinicaWeb.Models.Resultado>

@{
    ViewBag.Title = "Index";

    if (User.IsInRole("Admin"))
    {
        Layout = "~/Views/Shared/_LayoutSis.cshtml";
        ViewBag.A03 = "menu-open";
        ViewBag.A3 = "active";
        ViewBag.A3A = "active";
    }
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DetalleOrden.Cantidad)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetalleOrden.Cantidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

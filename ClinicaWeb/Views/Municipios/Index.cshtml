@model IEnumerable<ClinicaWeb.Models.Municipio>

@{
    ViewBag.Title = "Lista";
    Layout = "~/Views/Shared/_LayoutSis.cshtml";
    ViewBag.A9AA = "active";
    ViewBag.A9 = "active";
    ViewBag.A09 = "menu-open";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_mun)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departamento.Nombre_dpto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_mun)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento.Nombre_dpto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@model ClinicaWeb.Models.Sexo

@{
    ViewBag.Title = "Agregar Informacón";
    Layout = "~/Views/Shared/_LayoutSis.cshtml";
    ViewBag.A8 = "active";
}

<h3>Agregue Información</h3>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-10">
                <label> Tipo de Sexo</label>
                @Html.EditorFor(model => model.TipoSexo, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.TipoSexo, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Aceptar" class="btn btn-success" />
                <a href="@Url.Action("Index","sexoes")" class="btn btn-danger">Cancelar</a>
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
